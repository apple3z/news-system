# v2.6.2 问题清单

**版本**: v2.6.2

---

## 已知问题（从现状分析中发现）

| ID | 优先级 | 类型 | 描述 | 状态 |
|:--:|:------:|------|------|:----:|
| 1 | P0 | 功能缺陷 | "启动爬虫"仅爬取新闻，不包含Skills和RSS | ✅ 已修复 |
| 2 | P0 | 设计缺陷 | 前台订阅页做了管理功能而非内容展示 | ✅ 已修复 |
| 3 | P1 | 体验问题 | 后台三个管理列表风格不统一 | ✅ 已修复 |
| 4 | P1 | 安全缺陷 | 后台无登录认证，任何人可访问 | ✅ 已修复 |
| 5 | P2 | 架构问题 | 数据源硬编码在代码中，无法动态管理 | ✅ 已修复 |
| 6 | P2 | 架构问题 | 新闻爬虫用线程、Skills爬虫用子进程，不统一 | ✅ 已修复 |

---

## 开发中发现的问题

| ID | 优先级 | 类型 | 描述 | 状态 | 修复方式 |
|:--:|:------:|------|------|:----:|:--------:|
| BUG-001 | P0 | 功能缺陷 | news.db 的 crawl_log 表结构不匹配 | ✅ 已修复 | init_db.py 已统一表结构 |

---

## 测试中发现的8个问题（用户反馈）

| ID | 优先级 | 类型 | 描述 | 状态 | 修复文件 |
|:--:|:------:|------|------|:----:|:--------:|
| BUG-002 | P0 | 功能缺陷 | 后台新建数据源失败（无错误处理） | ✅ 已修复 | datasource_dal.py |
| BUG-003 | P0 | 显示缺陷 | 新闻卡片时间全显示"1分钟"，无发布日期 | ✅ 已修复 | news_dal.py, NewsView.js |
| BUG-004 | P0 | 数据缺陷 | 热门排行出现2023年旧新闻 | ✅ 已修复 | NewsView.js |
| BUG-005 | P0 | 功能缺陷 | 只有163和新浪有数据，其余8个源静默失败 | ✅ 已修复 | fetch_news.py |
| REQ-001 | P1 | 功能增强 | 采集日志缺少每条新闻的详细信息 | ✅ 已完成 | fetch_news.py, NewsAdmin.js |
| REQ-002 | P1 | 新功能 | 后台采集工作台需要每日数据分析曲线图 | ✅ 已完成 | NewsAdmin.js, news_dal.py, datasource_api.py |
| REQ-003 | P1 | 新功能 | Skills工具缺少排行榜 | ✅ 已完成 | SkillsView.js, skills_dal.py, skills_api.py |
| REQ-004 | P2 | UI优化 | 订阅页面改为新闻卡片样式 | ✅ 已完成 | SubscribeView.js |

---

## 问题详情

### BUG-002: 后台新建数据源失败

**根因**: `datasource_dal.py` 的 `create_source()` 方法无 try/except，数据库错误直接返回 HTML 500

**修复**: 给 `create_source()`、`update_source()`、`delete_source()` 加了 try/except 错误处理，返回 JSON 格式的错误信息

---

### BUG-003: 新闻卡片时间全显示"1分钟"

**根因（两个）**:
1. `news_dal.py` 的 `search_news()` SELECT 不包含 `content` 字段，前端 `getReadingTime(item.content)` 收到 undefined → 始终返回 "1分钟"
2. 卡片模板从未渲染 `item.time`（发布日期），只显示阅读时间

**修复**:
- `news_dal.py` — SELECT 加入 `content` 和 `image` 字段
- `NewsView.js` — 卡片 meta 区域新增显示 `item.time`（发布日期）

---

### BUG-004: 热门排行出现2023年旧新闻

**根因**: 热门查询 `sort_by=hot_score` 没有时间过滤，hot_score 无时间衰减

**修复**: `NewsView.js` — `loadHotNews()` 加了 `date_from` 参数（近30天）

---

### BUG-005: 只有163和新浪有数据

**根因（三个叠加）**:
1. 36kr/虎嗅/机器之心/量子位/CSDN等站点是JS渲染SPA，`requests.get()` + BeautifulSoup 拿不到内容
2. `get_page()` 用 bare `except` 吞掉所有错误，静默失败
3. `unique[:30]` 硬限制，163和新浪先调用占满配额

**修复**:
- 7个JS渲染站改为 RSS Feed 采集：36氪、虎嗅、机器之心、新智元、量子位、InfoQ、CSDN
- 新增 `fetch_from_rss()` 通用RSS解析函数（用 `xml.etree.ElementTree`，无新依赖）
- 新增 `parse_rss_time()` 解析 ISO 8601 + RFC 822 时间格式
- `get_page()` 改为打印错误日志，不再静默失败
- 配额从 `unique[:30]` 提升到 `unique[:100]`
- RSS源优先使用自带的时间和摘要数据

---

### REQ-001: 采集日志增加新闻详细信息

**修复**:
- `fetch_news()` 每条新闻记录 `{title, link, source, time, summary}` 存入 `crawl_log.error_message` 字段（JSON）
- `crawler_service.py` 将 news_detail 传入统一采集日志的 detail 字段
- `NewsAdmin.js` 日志表格新增"详情"列，可展开查看每条新闻的基础信息

---

### REQ-002: 每日数据分析曲线图

**修复**:
- `index.html` 添加 Chart.js 4.x CDN
- `news_dal.py` 新增 `get_daily_stats()` 和 `get_crawl_daily_stats()` 按日统计
- `datasource_api.py` 新增 `/api/admin/stats/daily` API 端点
- `NewsAdmin.js` 新增"数据分析"Tab，使用 Chart.js 渲染折线图（新闻发布量 + 采集新增量）

---

### REQ-003: Skills排行榜

**修复**:
- `skills_dal.py` 新增 `get_skill_rankings()` 按 stars DESC 排序
- `skills_api.py` 新增 `/api/skills/rankings` 公开端点
- `SkillsView.js` 右侧添加排行榜侧边栏，复用 `.hot-rank` CSS 样式

---

### REQ-004: 订阅页卡片样式

**修复**:
- `SubscribeView.js` 用 `.news-grid` + `.news-card` 替换原来的列表布局
- 复用 NewsView 的卡片 CSS 样式
- 添加 `parseTitle()` 方法从 content 中提取标题
- 使用 `pagination` 组件替代手写分页

---

**文档版本**: 2.0.0
**最后更新**: 2026-03-12
