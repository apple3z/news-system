# 技术设计 - v2.5.0

**版本**: v2.5.0  
**状态**: 📝 设计中  
**创建日期**: 2026-03-09  
**负责人**: 瑞贝卡 (PM)

---

## 一、技术选型

| 技术 | 选型 | 说明 |
|------|------|------|
| 后端框架 | Flask | 现有框架 |
| 前端 | HTML + JavaScript | 现有前端 |
| 数据存储 | 文件系统 + JSON | 物理文件 + .version.json |
| 版本管理 | 自定义函数 | 自动递增版本号 |

---

## 二、API 实现

### 2.1 保存文档 API

```python
@app.route('/api/v2/doc/save', methods=['POST'])
def save_document():
    """保存文档（统一 API，自动版本管理）"""
    data = request.json
    path = data.get('path')
    content = data.get('content')
    author = data.get('author', 'System')
    
    # 1. 验证路径安全
    if not is_safe_path(path):
        return jsonify({'code': 400, 'msg': '路径不安全'})
    
    # 2. 获取当前版本
    current_version = get_current_version(path)
    
    # 3. 递增版本号
    new_version = increment_version(current_version)
    
    # 4. 写入磁盘文件
    full_path = os.path.join(BASE_DIR, path)
    with open(full_path, 'w', encoding='utf-8') as f:
        f.write(content)
    
    # 5. 更新版本信息
    update_version_info(path, new_version, author)
    
    # 6. 清除缓存
    clear_cache(path)
    
    return jsonify({
        'code': 200,
        'version': new_version,
        'mtime': datetime.now().strftime('%Y-%m-%d %H:%M')
    })
```

### 2.2 读取文档 API

```python
@app.route('/api/v2/doc/read', methods=['GET'])
def read_document():
    """读取文档"""
    path = request.args.get('path')
    
    if not is_safe_path(path):
        return jsonify({'code': 400, 'msg': '路径不安全'})
    
    full_path = os.path.join(BASE_DIR, path)
    
    if not os.path.exists(full_path):
        return jsonify({'code': 404, 'msg': '文件不存在'})
    
    with open(full_path, 'r', encoding='utf-8') as f:
        content = f.read()
    
    # 获取版本信息
    version_info = get_version_info(path)
    
    return jsonify({
        'code': 200,
        'content': content,
        'version': version_info.get('version', '1.0.0'),
        'mtime': version_info.get('mtime', '')
    })
```

---

## 三、版本管理函数

### 3.1 递增版本号

```python
def increment_version(version):
    """递增版本号（修订号 +1）"""
    if not version:
        return '1.0.0'
    
    parts = version.split('.')
    if len(parts) == 3:
        parts[2] = str(int(parts[2]) + 1)
    else:
        parts = ['1', '0', '0']
    
    return '.'.join(parts)
```

### 3.2 获取当前版本

```python
def get_current_version(path):
    """获取文档当前版本号"""
    version_file = get_version_file_path(path)
    
    if os.path.exists(version_file):
        with open(version_file, 'r', encoding='utf-8') as f:
            version_data = json.load(f)
        
        filename = os.path.basename(path)
        if filename in version_data:
            return version_data[filename].get('version', '1.0.0')
    
    return '1.0.0'
```

### 3.3 更新版本信息

```python
def update_version_info(path, version, author):
    """更新版本信息到 .version.json"""
    version_file = get_version_file_path(path)
    
    # 读取现有版本信息
    if os.path.exists(version_file):
        with open(version_file, 'r', encoding='utf-8') as f:
            version_data = json.load(f)
    else:
        version_data = {}
    
    # 获取文件名
    filename = os.path.basename(path)
    
    # 更新版本信息
    version_data[filename] = {
        'version': version,
        'mtime': datetime.now().strftime('%Y-%m-%d %H:%M'),
        'author': author,
        'history': version_data.get(filename, {}).get('history', []) + [{
            'version': version,
            'mtime': datetime.now().strftime('%Y-%m-%d %H:%M'),
            'author': author
        }]
    }
    
    # 写入文件
    with open(version_file, 'w', encoding='utf-8') as f:
        json.dump(version_data, f, ensure_ascii=False, indent=2)
```

### 3.4 获取版本文件路径

```python
def get_version_file_path(doc_path):
    """获取版本文档对应的 .version.json 路径"""
    doc_dir = os.path.dirname(os.path.join(BASE_DIR, doc_path))
    return os.path.join(doc_dir, '.version.json')
```

---

## 四、辅助函数

### 4.1 路径安全验证

```python
def is_safe_path(path):
    """验证路径安全，禁止路径遍历攻击"""
    allowed_prefixes = ['版本历史/', '文档中心/']
    return any(path.startswith(prefix) for prefix in allowed_prefixes)
```

### 4.2 清除缓存

```python
def clear_cache(path):
    """清除相关缓存"""
    # 清除 Flask 缓存
    # 清除文件系统缓存
    pass
```

---

## 五、Web 端修改

### 5.1 保存时调用 API

```javascript
// sys_v2.js
async function saveDocument() {
    const response = await fetch('/api/v2/doc/save', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            path: currentDocPath,
            content: editorContent,
            author: '张振中'
        })
    });
    
    const result = await response.json();
    if (result.code === 200) {
        showToast('保存成功，版本号：' + result.version, 'success');
        // 更新版本号显示
        document.getElementById('version-display').textContent = result.version;
    } else {
        showToast('保存失败：' + result.msg, 'error');
    }
}
```

### 5.2 显示版本号

```html
<!-- sys_v2.html -->
<div class="doc-header">
    <span id="doc-title">文档标题</span>
    <span id="version-display">版本：1.0.0</span>
</div>
```

---

## 六、测试计划

### 6.1 单元测试

- [ ] 测试版本号递增逻辑
- [ ] 测试路径安全验证
- [ ] 测试版本信息持久化

### 6.2 集成测试

- [ ] AI 通过 API 保存文档
- [ ] Web 端保存后版本号递增
- [ ] 版本历史完整可追溯

### 6.3 协同测试

- [ ] AI 写入 → Web 读取
- [ ] Web 修改 → AI 读取
- [ ] 版本号正确显示

---

**文档版本**: v1.0  
**创建日期**: 2026-03-09  
**最后更新**: 2026-03-09（Phase 2 研发启动）
