# 架构设计文档 - v2.5.0

**版本**: v2.5.0  
**代号**: "稳定之基"  
**状态**: 🔄 设计中  
**创建日期**: 2026-03-06  
**负责人**: 瑞贝卡 (架构师)

---

## 一、问题分析

### 1.1 当前系统架构

```
┌─────────────────────────────────────────┐
│           Flask Web Server              │
│              (app.py)                   │
├─────────────────────────────────────────┤
│  ┌─────────────┐    ┌─────────────┐    │
│  │  版本历史    │    │  文档中心    │    │
│  │  (混乱)     │    │  (混乱)     │    │
│  └─────────────┘    └─────────────┘    │
├─────────────────────────────────────────┤
│  数据来源混乱：versions.json + 目录扫描  │
│  路径不统一：/doc/ + /版本管理/          │
│  排序错误：字符串排序而非语义排序        │
└─────────────────────────────────────────┘
```

### 1.2 核心问题清单

| 编号 | 问题 | 影响 | 根因 |
|:---:|------|------|------|
| B01 | 版本历史显示重复 | 界面混乱 | 同时从versions.json和目录读取 |
| B02 | 点击文档无响应 | 功能不可用 | 路径解析错误，混合使用/doc/和/版本管理/ |
| B03 | 版本顺序混乱 | 用户体验差 | 字符串排序，v2.10 < v2.5 |
| B04 | 文档列表不匹配 | 数据不一致 | 使用缓存，未实时刷新 |
| B05 | 保存后无法查看 | 数据不可靠 | 异步保存或缓存未清除 |
| B06 | 模块职责不清 | 架构混乱 | 版本管理和文档中心边界模糊 |

---

## 二、目标架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                    Flask Web Server                      │
│                     (app.py)                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │              统一版本管理层                      │   │
│  │         get_doc_version()                        │   │
│  │         save_doc_with_version()                  │   │
│  │              (统一函数)                          │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                               │
│         ┌───────────────┼───────────────┐               │
│         ▼               ▼               ▼               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │  版本管理    │  │  研发规范    │  │  其他目录    │     │
│  │  /版本管理   │  │  /docs      │  │  (可扩展)   │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  统一数据源：只从目录扫描（无缓存）                      │
│  统一路径：/版本管理/ 为版本文档根目录                   │
│  正确排序：语义化版本号排序（v2.10 > v2.5）              │
└─────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 物理路径 | Web端显示 | 职责 | 操作 |
|:---:|---------|:--------:|------|:----:|
| 版本管理 | `版本管理/` | 版本管理 | 历史版本归档 | 读写 |
| 研发规范 | `docs/` → `研发规范/` | 研发规范 | 现役规范文档 | 读写 |

---

## 三、详细设计

### 3.1 统一版本管理函数

#### 核心函数设计

```python
# utils/version_manager.py

import os
import json
import re
from datetime import datetime
from functools import cmp_to_key

VERSIONS_FILE = ".versions.json"

def parse_version(version_str):
    """
    解析版本号字符串为可比较的元组
    
    Args:
        version_str: 如 "v2.5.0" 或 "2.5.0" 或 "1.0.5"
    
    Returns:
        tuple: (major, minor, patch)
    """
    clean = version_str.lstrip('v')
    parts = clean.split('.')
    return tuple(int(p) for p in parts[:3])

def compare_versions(v1, v2):
    """
    比较两个版本号
    
    Returns:
        >0: v1 > v2
        <0: v1 < v2
        =0: v1 = v2
    """
    p1 = parse_version(v1)
    p2 = parse_version(v2)
    
    for a, b in zip(p1, p2):
        if a != b:
            return a - b
    return len(p1) - len(p2)

def get_sorted_versions(base_dir='版本管理'):
    """
    获取按版本号倒序排列的版本列表
    
    Returns:
        list: ['v2.6.0', 'v2.5.0', 'v2.4.0', ...]
    """
    versions = []
    
    if not os.path.exists(base_dir):
        return versions
    
    for name in os.listdir(base_dir):
        path = os.path.join(base_dir, name)
        if os.path.isdir(path):
            # 匹配版本号格式: v2.5.0 或 2.5.0
            if re.match(r'^v?\d+\.\d+(\.\d+)?$', name):
                versions.append(name)
    
    # 使用自定义比较函数排序（倒序）
    return sorted(versions, 
                  key=cmp_to_key(compare_versions), 
                  reverse=True)

def load_versions_meta(base_dir='.'):
    """
    加载版本元数据
    
    Returns:
        dict: 版本元数据字典
    """
    meta_path = os.path.join(base_dir, VERSIONS_FILE)
    if os.path.exists(meta_path):
        try:
            with open(meta_path, 'r', encoding='utf-8') as f:
                return json.load(f)
        except:
            pass
    return {}

def save_versions_meta(meta, base_dir='.'):
    """
    保存版本元数据
    """
    meta_path = os.path.join(base_dir, VERSIONS_FILE)
    with open(meta_path, 'w', encoding='utf-8') as f:
        json.dump(meta, f, ensure_ascii=False, indent=2)

def get_doc_version(filepath, base_dir='.'):
    """
    获取文档版本信息（统一函数）
    
    Args:
        filepath: 文档相对路径，如 "版本管理/v2.5.0/02-需求.md"
        base_dir: 基础目录
    
    Returns:
        {
            'version': '1.0.5',
            'mtime': '2026-03-06 14:30:00',
            'history': [{'version': '1.0.1', 'time': '...', 'action': '...'}],
            'locked': False,
            'last_update': '2026-03-06 14:30:00'
        }
    """
    full_path = os.path.join(base_dir, filepath)
    
    # 获取文件实际修改时间
    mtime = os.path.getmtime(full_path) if os.path.exists(full_path) else 0
    mtime_str = datetime.fromtimestamp(mtime).strftime('%Y-%m-%d %H:%M:%S')
    
    # 加载版本元数据
    meta = load_versions_meta(base_dir)
    doc_meta = meta.get(filepath, {})
    
    return {
        'version': f"{doc_meta.get('major', 1)}.{doc_meta.get('minor', 0)}.{doc_meta.get('patch', 0)}",
        'mtime': mtime_str,
        'history': doc_meta.get('history', []),
        'locked': doc_meta.get('locked', False),
        'last_update': doc_meta.get('last_update', mtime_str)
    }

def save_doc_with_version(filepath, content, base_dir='.', locked=None):
    """
    保存文档并自动更新版本（统一函数）
    
    Args:
        filepath: 文档路径
        content: 文档内容
        base_dir: 基础目录
        locked: 是否锁定（None表示保持原状态）
    
    Returns:
        {'code': 200, 'version': '1.0.6', 'msg': '保存成功'}
    """
    full_path = os.path.join(base_dir, filepath)
    
    # 确保目录存在
    dir_path = os.path.dirname(full_path)
    os.makedirs(dir_path, exist_ok=True)
    
    try:
        # 写入文件（同步）
        with open(full_path, 'w', encoding='utf-8') as f:
            f.write(content)
        
        # 更新版本元数据
        meta = load_versions_meta(base_dir)
        
        if filepath not in meta:
            meta[filepath] = {
                'major': 1,
                'minor': 0,
                'patch': 0,
                'history': [],
                'locked': False
            }
        
        doc = meta[filepath]
        
        # 检查锁定状态
        if doc.get('locked') and locked is None:
            return {'code': 403, 'msg': '文档已锁定，无法修改'}
        
        # 递增版本号（修订号）
        doc['patch'] = doc.get('patch', 0) + 1
        
        # 记录历史
        now = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        history = doc.get('history', [])
        history.append({
            'version': f"{doc['major']}.{doc['minor']}.{doc['patch']}",
            'time': now,
            'action': '更新'
        })
        doc['history'] = history[-20:]  # 只保留最近20条
        
        # 更新时间
        doc['last_update'] = now
        
        # 更新锁定状态
        if locked is not None:
            doc['locked'] = locked
        
        # 保存元数据
        save_versions_meta(meta, base_dir)
        
        return {
            'code': 200, 
            'version': f"{doc['major']}.{doc['minor']}.{doc['patch']}",
            'msg': '保存成功'
        }
        
    except Exception as e:
        return {'code': 500, 'msg': f'保存失败: {str(e)}'}
```

### 3.2 API接口设计

#### 版本列表接口

```python
@app.route('/api/sys/versions')
def api_versions():
    """
    获取版本列表（倒序）
    
    Response:
    {
        "code": 200,
        "data": [
            {
                "version": "v2.6.0",
                "path": "版本管理/v2.6.0",
                "docs": [...]
            },
            ...
        ]
    }
    """
    from utils.version_manager import get_sorted_versions, scan_version_docs
    
    versions = get_sorted_versions('版本管理')
    
    data = []
    for v in versions:
        data.append({
            'version': v,
            'path': f'版本管理/{v}',
            'docs': scan_version_docs(v, '版本管理')
        })
    
    return jsonify({'code': 200, 'data': data})
```

#### 文档读取接口

```python
@app.route('/api/wiki/raw/<path:filepath>')
def api_wiki_raw(filepath):
    """
    获取文档原始内容和版本信息
    
    Response:
    {
        "code": 200,
        "content": "# 文档内容...",
        "version": "1.0.5",
        "mtime": "2026-03-06 14:30:00",
        "locked": false
    }
    """
    from utils.version_manager import get_doc_version
    
    # 安全检查
    if '..' in filepath or filepath.startswith('/'):
        return jsonify({'code': 400, 'msg': '无效的路径'}), 400
    
    # 构建完整路径
    full_path = os.path.join(BASE_DIR, filepath)
    
    if not os.path.exists(full_path):
        return jsonify({'code': 404, 'msg': '文件不存在'}), 404
    
    try:
        with open(full_path, 'r', encoding='utf-8') as f:
            content = f.read()
        
        # 获取版本信息
        version_info = get_doc_version(filepath, BASE_DIR)
        
        return jsonify({
            'code': 200,
            'content': content,
            'version': version_info['version'],
            'mtime': version_info['mtime'],
            'locked': version_info['locked']
        })
        
    except Exception as e:
        return jsonify({'code': 500, 'msg': str(e)}), 500
```

#### 文档保存接口

```python
@app.route('/api/wiki/save', methods=['POST'])
def api_wiki_save():
    """
    保存文档并更新版本
    
    Body:
    {
        "filepath": "版本管理/v2.5.0/02-需求.md",
        "content": "# 更新后的内容...",
        "locked": false
    }
    
    Response:
    {
        "code": 200,
        "version": "1.0.6",
        "msg": "保存成功"
    }
    """
    from utils.version_manager import save_doc_with_version
    
    data = request.get_json()
    
    if not data:
        return jsonify({'code': 400, 'msg': '请求体为空'}), 400
    
    filepath = data.get('filepath')
    content = data.get('content')
    locked = data.get('locked')
    
    if not filepath or content is None:
        return jsonify({'code': 400, 'msg': '缺少filepath或content'}), 400
    
    # 安全检查
    if '..' in filepath or filepath.startswith('/'):
        return jsonify({'code': 400, 'msg': '无效的路径'}), 400
    
    # 只允许特定目录
    allowed_prefixes = ['版本管理/', 'docs/', '研发规范/']
    if not any(filepath.startswith(p) for p in allowed_prefixes):
        return jsonify({'code': 403, 'msg': '不允许访问该路径'}), 403
    
    # 保存文档
    result = save_doc_with_version(filepath, content, BASE_DIR, locked)
    
    return jsonify(result)
```

### 3.3 前端修改

#### 版本树渲染（修复重复显示）

```javascript
// static/sys.js

// 全局变量，避免重复初始化
let versionsLoaded = false;

async function loadVersionTree() {
    // 防止重复加载
    if (versionsLoaded) {
        console.log('版本树已加载，跳过');
        return;
    }
    
    const response = await fetch('/api/sys/versions');
    const result = await response.json();
    
    if (result.code !== 200) {
        console.error('加载版本列表失败:', result.msg);
        return;
    }
    
    const treeContainer = document.getElementById('version-tree');
    
    // 清空容器（确保无重复）
    treeContainer.innerHTML = '';
    
    // 标记已加载
    versionsLoaded = true;
    
    // 遍历版本（已经是倒序）
    for (const version of result.data) {
        const versionNode = createVersionNode(version);
        treeContainer.appendChild(versionNode);
    }
}
```

#### 文档加载（修复路径问题）

```javascript
let currentDoc = null;
let currentVersionInfo = null;

async function loadDoc(filepath) {
    currentDoc = filepath;
    
    const response = await fetch(`/api/wiki/raw/${encodeURIComponent(filepath)}`);
    const result = await response.json();
    
    if (result.code === 200) {
        document.getElementById('editor').value = result.content;
        document.getElementById('doc-title').textContent = filepath;
        
        // 显示版本信息
        currentVersionInfo = {
            version: result.version,
            mtime: result.mtime,
            locked: result.locked
        };
        updateVersionDisplay();
    } else {
        showToast(`加载失败: ${result.msg}`, 'error');
    }
}

function updateVersionDisplay() {
    if (!currentVersionInfo) return;
    
    const versionEl = document.getElementById('doc-version');
    const timeEl = document.getElementById('doc-mtime');
    const lockEl = document.getElementById('doc-lock');
    
    if (versionEl) versionEl.textContent = `版本: v${currentVersionInfo.version}`;
    if (timeEl) timeEl.textContent = `更新: ${currentVersionInfo.mtime}`;
    if (lockEl) {
        lockEl.textContent = currentVersionInfo.locked ? '🔒 已锁定' : '🔓 未锁定';
        lockEl.className = currentVersionInfo.locked ? 'locked' : 'unlocked';
    }
}
```

#### 保存功能（添加反馈）

```javascript
async function saveDoc() {
    if (!currentDoc) {
        showToast('请先选择文档', 'warning');
        return;
    }
    
    const content = document.getElementById('editor').value;
    const lockCheckbox = document.getElementById('lock-checkbox');
    
    showToast('保存中...', 'info');
    
    const response = await fetch('/api/wiki/save', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            filepath: currentDoc,
            content: content,
            locked: lockCheckbox ? lockCheckbox.checked : undefined
        })
    });
    
    const result = await response.json();
    
    if (result.code === 200) {
        showToast(`保存成功 (版本: v${result.version})`, 'success');
        // 更新本地版本信息
        if (currentVersionInfo) {
            currentVersionInfo.version = result.version;
            updateVersionDisplay();
        }
    } else {
        showToast(`保存失败: ${result.msg}`, 'error');
    }
}

// Toast提示函数
function showToast(msg, type = 'info') {
    const toast = document.createElement('div');
    toast.className = `toast toast-${type}`;
    toast.textContent = msg;
    document.body.appendChild(toast);
    
    // 动画显示
    setTimeout(() => toast.classList.add('show'), 10);
    
    // 3秒后消失
    setTimeout(() => {
        toast.classList.remove('show');
        setTimeout(() => toast.remove(), 300);
    }, 3000);
}
```

---

## 四、数据流设计

### 4.1 版本列表面览流程

```
用户访问 /sys
    │
    ▼
前端调用 GET /api/sys/versions
    │
    ▼
后端 get_sorted_versions('版本管理')
    │
    ├── 扫描 /版本管理/ 目录
    ├── 提取版本号（正则匹配）
    ├── 语义化排序（v2.10 > v2.5）
    └── 返回排序后的版本列表
    │
    ▼
前端渲染版本树（只渲染一次，防重复）
```

### 4.2 文档保存流程

```
用户编辑文档 → 点击保存
    │
    ▼
前端 POST /api/wiki/save
    │
    ├── 验证路径安全性
    ├── 验证目录权限
    └── 发送内容到后端
    │
    ▼
后端 save_doc_with_version()
    │
    ├── 同步写入磁盘文件
    ├── 更新 .versions.json 元数据
    │   ├── 版本号 +1
    │   ├── 记录历史
    │   └── 更新时间
    └── 返回新版本号
    │
    ▼
前端显示"保存成功"提示
前端更新版本号显示
```

---

## 五、安全设计

### 5.1 路径安全

```python
def validate_path(filepath):
    """验证路径安全性"""
    # 禁止路径遍历
    if '..' in filepath:
        return False, '路径包含非法字符'
    
    # 禁止绝对路径
    if filepath.startswith('/'):
        return False, '不允许绝对路径'
    
    # 只允许特定目录
    allowed = ['版本管理/', 'docs/', '研发规范/']
    if not any(filepath.startswith(p) for p in allowed):
        return False, '不允许访问该目录'
    
    return True, None
```

### 5.2 文件类型限制

```python
ALLOWED_EXTENSIONS = {'.md', '.txt', '.json'}

def validate_filetype(filename):
    ext = os.path.splitext(filename)[1].lower()
    return ext in ALLOWED_EXTENSIONS
```

---

## 六、部署架构

### 6.1 文件变更清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `utils/version_manager.py` | 新增 | 统一版本管理函数 |
| `app.py` | 修改 | 更新API路由，使用新函数 |
| `static/sys.js` | 修改 | 修复前端交互问题 |
| `templates/sys.html` | 修改 | 优化界面布局 |

### 6.2 回滚方案

如需回滚：
1. 恢复 app.py 备份
2. 删除 utils/version_manager.py
3. 重启服务

---

## 七、验收标准

### 7.1 功能验收

- [ ] 版本列表只显示一次，无重复
- [ ] 版本按 v2.6 > v2.5 > v2.4 倒序排列
- [ ] 点击任意版本文档能正常打开
- [ ] 保存后立即在Web端看到更新
- [ ] 版本号和修改时间实时显示

### 7.2 性能验收

- [ ] 页面加载 < 2秒
- [ ] 文档保存 < 1秒
- [ ] 版本排序算法 < 100ms

### 7.3 质量验收

- [ ] 无JavaScript报错
- [ ] 无后端Python异常
- [ ] 所有测试用例通过

---

**文档版本**: v1.0  
**创建日期**: 2026-03-06  
**最后更新**: 2026-03-06
